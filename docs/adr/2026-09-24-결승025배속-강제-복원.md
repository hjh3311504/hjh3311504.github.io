# 결승0.25배속 강제 복원

- 날짜: 2026-09-24
- 상태: 적용
- 대체 결정: [결승 자동 감속과 선택한2배속 유지](2026-09-24-결승-자동감속과-2배속-유지.md)의2배속 예외를 철회한다.

## 배경

사용자는 결승0.25배속 강제가 필수라고 정정했다. 이전 작업은 의도한 슬로모션을 성능 문제의 원인으로 잘못 판단했다. 당시 측정으로 사용자가 보고한 실제 렉을 재현하거나 해결했다고 볼 수 없다.

## 결정

- 선택한 배속과 관계없이 결승 연출 중에는0.25배속을 강제한다.
- Worker 계산, 다음 요청 대기 시간, 배속 버튼과 접근성 안내를 이전 구현으로 복원한다.
- 결승 연출 종료 뒤 선택한 배속으로 복귀한다. 연출 중에는 배속 전환을 잠근다.
- FAQ, 요구사항, 화면 문서와 브라우저 검사의2배속 예외를 제거한다.
- 기존 물리·성능 개선과 회전바·경사·블록 상한·종료 뒤 초기화 변경은 보존한다.

## 한계

이번 변경은 잘못 추가한 정책의 복원이다.1000개 경기의 실제 렉 문제를 해결한 성능 개선으로 보고하지 않는다. 후속 분석에서는 필수0.25배속 연출과 계산 지연을 구분해야 한다.

## 검증

- Worker·당첨 범위 단위 테스트9개 통과. 실제 계산에서 결승0.25배속과 연출 종료 뒤2배속 복귀, 당첨 이벤트 일치를 확인했다.
- 관련 Playwright5개 통과. 접근성 snapshot을 먼저 확인하고 데스크톱·모바일 표시, 결승 확대, 일시정지·재개, 연출 종료 뒤 배속 복귀를 검사했다.
- 정적 검사 오류·경고0개. 변경 파일 lint·포맷, UI 검사, REQ-MARBLE-003·화면 연결 검사와 정적 build 검증 통과.
- 최종 build는1회 수행했다. 전체 테스트와 물리 밀집 검사는 반복하지 않았다. commit·push·배포는 수행하지 않았다.
