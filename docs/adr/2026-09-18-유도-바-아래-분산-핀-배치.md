# 유도 바 아래 분산 핀 배치

- 날짜: 2026-09-18
- 상태: 적용
- 근거: 사용자가 분산 핀을 사이드 구슬을 모으는 바 아래로 옮기도록 요청했다.

구역 사이3곳은 유도 바 → 분산 핀 → 특수 블록 순서로 배치한다. 유도 바는 유지하고 핀3줄을 아래로 옮긴다. 구간 시작점 기준 핀 중심 높이는478·570·632다. 버터는740·960, 연못은890 높이에 두어 핀과 겹치지 않게 한다. 바와 핀, 핀과 특수 블록 사이의 세로 틈은 구슬 지름26보다 넓다.

구역 높이1220·핀52개·연못3분할·우회로2개·결승 앞 핀은 유지한다. 직전 ADR의 핀 → 유도 바 순서를 이번 배치로 대체한다.

관련 단위 검사14개와 데스크톱·모바일 미니맵 배치 검사를 통과했다. 기본3개 맵에서2/30/60명, 난수47의9경기를 확인했으며 모두 완주했다. 최대 완주 시간은77.37경기초였다. lint·정적 검사·build·정적 산출물·문서 연결 검사도 통과했다. 기존 결승 체류시간·밀집 검사의 한계는 이번 표본 완주 검사로 해소됐다고 판단하지 않는다.
