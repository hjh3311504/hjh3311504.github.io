# 전송 비교와 보간 기록 축소

- 날짜:2026-09-26
- 상태: 적용, 원격 재검증 예정
- 관련: [충돌의 빠른 제외와 화면 그림 조회 재사용](2026-09-26-충돌-빠른-제외와-화면-그림-조회-재사용.md)

## 직전 원격 결과

`a762220`의 [실행36241015088](https://github.com/hjh3311504/hjh3311504.github.io/actions/runs/36241015088)은 성능 기준 미달로 실패했다. lint·UI 규칙·정적 검사·전체 단위 검사·build는 통과했다. 우선 브라우저8개 중5개 통과·3개 실패였고, 뒤의 전체 브라우저 검사와 정적 결과 검증은 실행되지 않았다.

| 자율 Worker·2배속  | 출발 실제 배속     | 진행 실제 배속     | 출발 fps     | 진행 fps     |
| ------------------ | ------------------ | ------------------ | ------------ | ------------ |
| 데스크톱           | 1.902배에서1.942배 | 1.697배에서1.850배 | 46.0에서46.8 | 53.8에서54.3 |
| 모바일·CPU4배 감속 | 2.071배에서2.104배 | 2.009배에서2.027배 | 20.1에서20.4 | 24.5에서25.0 |

최초와재시도2회의 범위다. 모바일1배속도 출발14.1fps에서14.7fps, 진행20.0fps에서20.4fps로 미달했다. 모바일2배속 최초 출발 표본2.104배는 상한2.1배도 넘었다. 화면 전달 지연과 초기 따라잡기 영향을 포함하므로 실제2배속 검사를 완전히 통과했다고 표현하지 않는다.

별도 계측에서 데스크톱 물리1단계는 출발3.69ms·진행4.65ms, 전송 상태 구성은 약1.0ms에서1.1ms였다. 모바일 Canvas 함수는 약8.05ms에서10.35ms였지만 전체 화면 간격을 설명하기에는 부족했다. 로컬 CPU12배 감속의 작업 기록과 원격 CPU4배 감속을 같은 환경으로 취급하지 않는다.

같은 실행 장비에서 이전 요청 방식을 현재 물리 코드로 돌린 별도 기준4개도1개 통과·3개 실패였다. `continue-on-error`의 단계 success를 검사 통과로 해석하지 않는다. 이전 요청 방식은 데스크톱2배속 진행1.573배, 모바일2배속 진행1.620배였다.

`a762220`의 로컬 실제 결승은105경기초부터 실제10초씩2회 측정했다. 데스크톱2.002배·2.001배와 약60fps, 모바일 CPU4배 감속2.001배·2.002배와58.17fps·57.28fps였다. 결승에263개에서292개가 있었고 필수 감속 연출은 없었다. 일반 블록과 스킬·소리를 유지한 실제 경기다.

## 추가 결정

- 일반 블록 탐색 격자를68에서48로 줄인다. 후보는 기존 블록 번호순으로 처리한다. 경기 지형의 크기를 바꾸는 값이 아니다.
- 빈 접촉 기록을 순회하지 않는다. 첫 진입선보다 위에 있는 구슬은 통과 기록의 전체 후보를 확인하지 않는다.
- 충돌의 두 좌표 거리는 큰 좌표로 정규화한 뒤 계산한다. 큰 값의 제곱 넘침과 작은 값의 소실을 피한다. 특수 값은 `Math.hypot`에 맡긴다. 기본 함수와 같은 결과인지70,000개 좌표 쌍과 특수 값으로 검사한다. 단순 제곱합의 제곱근으로 바꾸어 기존 경기 경로가 달라지는 실험은 채택하지 않았다.
- 전송할 블록의9개 속성이 모두 있으면 각 속성을 직접 비교한다. 속성이 적은 블록은 기존 목록 순회를 유지한다. `Object.is`를 유지해 `NaN`과 부호가 다른0도 구분한다.
- 보간의 이전·최신 기록에는 위치·도착·고정 정보만 담는다. 화면 구슬에는 최신 전체 정보를 복사한다. 실제 구슬과 고정 위치를 공유해 수정하지 않는다.
- 같은 화면 구간의 블록 후보 배열과 같은 모양의 타일 그림 조회를 재사용한다. 파괴 잔상의 블록은 전체 배열 대신 번호로 찾는다.
- 원격 성능 실패 때만 모바일1배속의 별도 Chromium 작업 기록을 압축해 `marble-mobile-trace`에 남긴다. 일반 검사와 기존 비용 계측 뒤에 실행한다. 일반 성능 기준과 실패 처리는 그대로다.

## 같은 입력의 로컬 비교

Chromium의1000개·난수47·스킬ON·2400단계 비교에서 격자와 빈 순회 축소는 준비 뒤 약2051ms에서2082ms를1893ms에서1905ms로 줄였다. 추가 거리 계산 비교는 약1860ms에서1862ms를1748ms에서1805ms로 줄였다. 각 비교에서 구슬·블록·도착·시각·이벤트의 최종 상태가 정확히 일치했다. 별도 실험끼리 실행 시간이 다르므로 감소율을 단순 합하지 않는다.

Node의1000개 상태1000회 구성 비교는 약227ms에서238ms를93ms에서99ms로 줄였다. 표시 상태 복사·보간1000회 비교는 약104ms에서106ms를32ms에서35ms로 줄였다. 두 결과는 전체 경기 속도의 개선율이 아니다.

이전 `d7daa93`과 실제105경기초 저장 상태를600단계 계산한 비교에서도 최종 위치·도착106개와 모든 이벤트 이력이 정확히 같았다. 준비 뒤 단계 평균은 이전1.682ms·1.729ms, 변경1.646ms·1.620ms였다. 결승 자체의 추가 감소는 출발 구간보다 작다.

블록을 큰 행 그림으로 묶는 방식, 미니맵 점을 그림으로 바꾸는 방식, 글자 그림을 한 페이지에 모으는 방식은 충분한 개선이 없거나 느려져 채택하지 않았다. 화면 해상도·효과·표시 규칙을 낮추지 않는다. 필수0.25배속, 물리 단계, 지형·회전바·반발계수·스킬 규칙과 경기 시간 보존도 유지한다.

## 검증

변경 로직 단위23개와 결승·벽·스킬·배속·Worker 관련49개가 통과했다.1000개 밀집의 실제 겹침과 표시 겹침은0.5 미만이었다.1000개·회전각4가지·35경기초 벽 통과 방지, 중앙 통로 골인, 번개 고정의 회전바 밀림과 얼음 고정,0.25·1·2배속 재현성도 통과했다.

확대·추적·새 경기·화면 크기·블록 파괴·크기 변경·보이는 구간 변경13개 그림 비교의 픽셀 차이가0이었다. 정적 검사 오류·경고0개와 workflow 문법도 확인했다. 원격 실패 뒤 제품 코드를 추가 수정했으므로 해당 코드의 최종 build를 다시1회 만들고 후속 브라우저 검사에 재사용한다.

해당 build에서 관련 브라우저28개와 별도 작업 기록 수집1개도 통과했다. 일반 출발·진행2배속은 데스크톱2.004배·1.998배와 약60fps, 모바일 CPU4배 감속2.005배·2.000배와54.09fps·60.04fps였다. 압축 기록에 실제 화면 프레임과 함수 표본이 들어 있는 것도 확인했다. 이 로컬 수치로 원격 기준 통과를 대신하지 않는다.

원본은 `.context/collision-render/`의 `remote.log`, `finale-live.log`, `tuned-browser.log`, `hypot-browser.log`, `encoder.log`, `presentation.log`, `followup-finale-compare.log`, `followup-units.log`, `followup-safety.log`, `followup-pixels.log`다. 후속 로컬·원격 결과도 같은 폴더에 따로 보관한다.
